# Changelog

## 0.2.0 (2026-09-25)

- Downloadable binaries for Windows, macOS (Apple Silicon and Intel) and Linux
  on every release, with `SHA256SUMS.txt`, so anyone can run their own copy.
- Published to crates.io: `cargo install tech-oracle`.
- `tech-oracle --help` and `--version`. Running with no arguments (how the
  daily build runs it) is unchanged.
- MIT licence metadata.

## 0.1.0

- The engine behind THE SIGNAL: fetch, measure, generate, grade, render.
